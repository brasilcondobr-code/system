/**
import './bootstrap';
import Alpine from 'alpinejs';
window.Alpine = Alpine;
Alpine.start();
**/

// Importa o Bootstrap e Popper para funcionalidades JS (como modais, tooltips, etc.)
import * as bootstrap from 'bootstrap';
import * as Popper from '@popperjs/core';

// Código de inicialização do seu aplicativo (ex: eventos, inicialização do AdminLTE)
console.log('Brasil Condo - Frontend Loaded');
